# dev-playbook
Basic linux environment bootstrapping, using roles from https://github.com/geerlingguy/pi-dev-playbook

Install dependencies `ansible-galaxy install -r requirements.yml`
Install the stuff `ansible-playbook main.yml -c local`

